# warning-agent architecture clarity optimization successor replan input

- predecessor_plan: `warning-agent-architecture-clarity-optimization-2026-04-20`
- predecessor_closeout: `docs/plan/warning-agent-architecture-clarity-optimization-2026-04-20_CLOSEOUT.md`
- generated_at: `2026-04-20`
- intended_handoff: `plan-creator`

## 1. What is already closed

The current pack already landed, with evidence:

- explicit architecture-clarity guardrails and target map
- clearer `3.5` runtime/training/corpus ownership
- minimal analyzer assist/audit internal records
- clearer `3.6` local-primary resident-lifecycle seam
- clearer `3.6` cloud-fallback brief/request seam
- narrower normalized-alert dependency direction
- minimal investigator internal records for later learning optimization

## 2. Residuals intentionally not claimed as closed

1. deeper `3.5` policy optimization (query rewrite / ambiguity / investigation value / calibration uplift)
2. deeper `3.6` action-trace / evidence-pack / stop-policy / compression-policy optimization
3. stronger compare surfaces that use the new internal objects as first-class offline evidence
4. any eventual extraction gate toward `warning-core`

## 3. Honest next-pack candidates

### candidate A — `3.5` learning optimization pack

Use when the next goal is to improve first-pass policy quality without reopening contracts.

Focus:

- assist-informed retrieval rewrite
- ambiguity estimation
- investigation-value prediction
- better audit / compare summaries
- offline benchmark-backed policy uplift

### candidate B — `3.6` action-trace and evidence-pack optimization pack

Use when the next goal is to improve investigator efficiency/clarity rather than product scope.

Focus:

- stronger `ActionTrace`
- richer `InvestigationEvidencePack`
- stop-policy summary
- compression-policy summary
- premium-invocation value analysis

### candidate C — extraction-gate definition pack

Use only when `3.5/3.6` internal objects and compare surfaces are stable enough to discuss a later `warning-core` boundary.

Focus:

- extraction criteria
- migration guardrails
- what remains repo-local vs what can be moved

## 4. Replan guardrails

The successor must continue to preserve:

- canonical contracts unchanged by default
- current provider topology unchanged by default
- keep-the-shell rule
- minimal internal objects first
- fail-closed over pretend-live
- no generic state/policy/runtime framework unless that is the explicit new boundary
